<Window x:Class="ClientBase.DownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обновление программы" Width="460" Height="210" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" FontSize="14"
        Background="{StaticResource BgBrush}" Foreground="{StaticResource TextBrush}"
        Closing="Window_Closing">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" FontSize="17" FontWeight="SemiBold" TextWrapping="Wrap"/>
        <ProgressBar x:Name="Bar" Grid.Row="1" Height="10" Margin="0,16,0,6" Minimum="0" Maximum="1"
                     Foreground="{StaticResource AccentBrush}" IsIndeterminate="True"/>
        <TextBlock x:Name="SizeText" Grid.Row="1" VerticalAlignment="Bottom" Margin="0,0,0,-22"
                   Foreground="{StaticResource MutedBrush}" FontSize="12.5"/>
        <Button Grid.Row="2" Content="Отмена" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Click="Cancel_Click"/>
    </Grid>
</Window>
